export const EVENT_TYPES = [
  'Project Work',
  'Admin Work',
  'Marketing',
  'Annual Leave',
  'Medical Leave (MC)',
  'Emergency Leave',
  'Unpaid Leave',
]

// Leave-type entries span the whole day rather than logged hours — the
// Activities Summary page uses this to render them without a duration.
export const LEAVE_EVENT_TYPES = ['Annual Leave', 'Medical Leave (MC)', 'Emergency Leave', 'Unpaid Leave']
